                                                  Array
1) In Array Copy Operations make shallow copies(shares the memeory refrences or address) not deep copies(make's diffrent copy)                                                   
2) Collections of different types of data
3) Variable size + mutable
                          Declaring An Array
                       let arr = [];
                    let arr = new Array(5);


                     Functions In Array
(A) .length
(C) .unshift - not optimized way because for adding a single element at the begining it shift's all the elements of an array
(D) .shift - remove's element from start
(E) .push - add element at last 
(F) .pop - remove's last element
(G) Array.isArray("Hitesh") - to check whether the give value is array or not
(H) Array.from("Hatim") - will convert the given string into array of chars
(I) Array.of() - can pass multiple elements and it will be converted into array 
(J) .concat() - joins two arrays and return a new array doesn't manipulate the actual array
(K) let arr = [...arr1 , ...arr2 , arr3] - will spread all the given array into a single array can use instead of concat when concatinating multiple arrays
(L) .join() - convert's array into string using a separator(ex : join("And") , join("or")) 
(M) .reverse
(N) .delete - it delete the element from particular position but length of an array will remain same
(O) .flat() - flats the array - it needs a value ex : 1,2 etc that is no of nested array's we have to flat and return a new array
(p) const arr = Object.keys(obj) - give array of object keys , Object.values(obj) - array of object values , Object.entries(obj) - arrays of[key,value]
(Q) slice - return a range of array , keeps the actual array same
(R) splice - return a range of array , and also remove the selected range from  array

                                         Questions
1) let arr = [1,2,3] , let arr2 = [3,5] 
arr.push(arr2) - it will push the complete at last index of arr (arr[3] will be an array)

2) let arr = [1,2,4]; arr[3] - will give undefined instead of  an error
3) data type of array - object
4) console.log(Array.from({name : "Hello"})) - give's empty array  

                                                     Objects In Js


let mySym = Symbol('key1')  - using symbol as a key in object                                          
let obj = {
    name : "Hello",
    age : 21,
    "full name" : "Hello World",
    leaves : ["monday" , "friday"],
    [mySym] : "MyKey1"; - this key will be treated as symbol
    mySym : "MyKey1"; - this key  will be treated as normal string
}
// Acessing value's in object
console.log(obj.name) - using .keyword
console.log(obj["full name"]) - using sqaure bracs , we can't access this key with .keyword
console.log(obj[mySym]) - it's a symbol so we can use it wihtout double quotes

Object.freeze(obj) - it make's oject value's remain same , any changes after this won't reflect


Topics Covered : 
1) Arrays ✅
2) Object ✅